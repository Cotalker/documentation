import Mermaid from '@theme/Mermaid';



<div className="align-center>

<Mermaid chart={`
    graph TD;
        EQ(Equipo)
        CO(Centro Operativo) --> EQ
        TCO(Tipo Centro Operativo) --> CO
        CO --> M(Material)
        style M fill:#f799e3
`}/>

</div>